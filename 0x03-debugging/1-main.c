#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("infinite loop incoming :(\n");
/**
 * while - i = 0;
 *
 *       while - (i < 10)
 *  {
 *   putchar(i);
 * @'\\0/\n'":  }
 */
	printf("Infinite loop avoided! \\0/\n");

	return (0);
}
