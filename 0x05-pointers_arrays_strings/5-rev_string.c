#include "main.h"
/**
 * rev_string- reverses a given string
 * @s: string to be tested
 *
 * Return: void
 */
void rev_string(char *s)
{
	int x, y, z;
	char i, j;

	for (x = 0; s[x] != '\0'; x++)
	{
	}
	y = x - 1;
	z = 0;
	while (y >= z)
	{
		z = s[y];
		y--;
	}
}

