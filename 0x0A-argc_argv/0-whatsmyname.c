#include <stdlib.h>
#include <stdio.h>
/**
 * main- entry point to run the code
 * @argc: the number of argument present
 * @argv: the elements of arguments present
 * Return: returns an integer
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
