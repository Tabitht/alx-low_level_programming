#include <stdio.h>
/**
 * main- the entry point to enable the code run
 *
 *
 * Return: will always return 0
 */
int main(void)
{
	char ch = 'a';
while (ch <= 'z')
{
putchar(ch);
ch++;
}
putchar(\n);
	return (0);
}
