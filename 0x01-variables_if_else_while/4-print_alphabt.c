#include <stdio.h>
/**
  *main- entry point to run the code
  *
  *
  * Return: to always return 0
  */
int main(void)
{
	char ch = "a"
while (ch <= "z")
{
putchar(ch)
if ((ch == "q") || (ch == "e"))


