#include <stdio.h>
/**
* main- entry point for running the code
*
*
* Return: to always return 0
*/
int main(void)
{
	char ch = 'z';
while (ch >= 'a')
{
putchar(ch);
ch--;
}
putchar('\n');

	return (0);
}
