#include <stdio.h>
/**
 * main- entry point to run the code
 *
 *
 * Return: to always return 0
 */
int main(void)
{
	int c, i;
for (c = 97; c <= 122; c++)
{
putchar(c);
}
for (i = 65; i <= 90; i++)
{
putchar(i);
}
putchar('\n');

	return (0);
}
